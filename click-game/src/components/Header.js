import React from "react";
import "./Header.css";

const Header = () => (
  <header className="header">
    <h1 className="title">Smash Ultimate Clicky Game</h1>
    <h3>
      Click on a player to earn points, but don't click on any more than once!
    </h3>
  </header>
);

export default Header;
